<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cberthelot.openclasstutorial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- android:theme="@style/AppTheme"> -->
        <activity android:name=".fragment.FragmentListActivity" />
        <activity android:name=".fragcommunication.CountriesActivity" />
        <activity android:name=".fragment.SettingsWithHeaderActivity" />
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity>&lt;!&ndash; ATTENTION: This was auto-generated to add Google Play services to your project for -->
        <!-- App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. &ndash;&gt; -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- <activity android:name=".fragment.SettingsWithHeaderActivity"></activity> -->
        <activity android:name=".dialog.DialogActivity" />
        <activity android:name=".toolbar.MyActivity" />
    </application>

</manifest>